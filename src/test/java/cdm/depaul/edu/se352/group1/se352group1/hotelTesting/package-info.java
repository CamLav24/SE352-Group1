/**
 * <b>testHotelId:</b>
 *
 * Verifies that the hotel ID can be set and retrieved correctly, including handling negative values.
 * testName:
 *
 * Ensures the hotel name can be set and retrieved, correctly handles null and empty values, and verifies behavior with extremely long names.
 * <b>testLocationId:</b>
 *
 * Confirms the location ID can be set and retrieved correctly, including handling negative values.
 * <b>testPrice:</b>
 *
 * Checks that the price can be set and retrieved correctly, handles zero and negative values, and verifies behavior when the price is null.
 *<b>testRoomsLeft:</b>
 *
 * Validates that the number of rooms left can be set and retrieved correctly, including handling zero, negative values, and null.
 *
 * @author Cam
 */
package cdm.depaul.edu.se352.group1.se352group1.hotelTesting;
